package: api
generate:
  chi-server: true
  models: true
  embedded-spec: true
# The output path is passed with -o by the //go:generate directive in
# internal/api/handlers.go, which runs from that package's directory.
